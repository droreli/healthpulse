Drop redacted real HAE exports into this folder when available.

Recommended names:
- `real-metrics.json`
- `real-workouts.json`

The automated tests use the PRD-derived fixtures today. Real exports should be added as additional regression inputs, not replacements.
